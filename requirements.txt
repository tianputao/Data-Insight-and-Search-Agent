# MAF Data Insight Agent — Python Dependencies

# ── Core Framework (Microsoft Agent Framework) ────────────────────────────────
agent-framework-core==1.11.0
agent-framework-openai==1.10.1

# ── Azure Services ────────────────────────────────────────────────────────────
azure-search-documents>=11.4.0
azure-core>=1.29.0
azure-identity>=1.15.0

# ── OpenAI (Azure OpenAI) ─────────────────────────────────────────────────────
openai>=2.25.0,<3

# ── FastAPI backend ────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0           # async HTTP client used by FastAPI test client
requests>=2.31.0        # direct dependency of the Blob image proxy endpoint

# ── Environment Management ────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Azure Databricks ─────────────────────────────────────────────────────────
databricks-sql-connector>=3.0.0    # Python DB-API/Thrift SQL execution against Databricks warehouse
databricks-sdk>=0.20.0             # Unity Catalog REST API (table/schema metadata)
sqlglot>=30.13.0,<31.0.0           # Parse Databricks SQL for catalog/schema scope enforcement

# ── Data Processing ───────────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0                      # required by databricks-sql-connector result handling

# ── Ontology Runtime ──────────────────────────────────────────────────────────
owlready2>=0.48,<1.0               # reasoning (HermiT/Pellet) also requires Java 11+

# ── Utilities ─────────────────────────────────────────────────────────────────
pydantic>=2.5.0
typing-extensions>=4.9.0

# ── Development and Test Tools (installed by this requirements file) ─────────
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.12.0
flake8>=7.0.0
